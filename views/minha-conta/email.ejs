<%- include('../components/head', {title: 'Minha Conta: Alterar E-mail'}) %> <%- include('../components/navbar') %>

<section>
  <div class="corpo container-fluid text-branco">
    <div class="row g-1">

      <%- include('../components/minha-conta/lateral-menu') %>

      <div class="col-sm px-3">

        <h4>Alterar E-mail</h4>      
        <hr class="mb-3">

        <div class="m-w-800">

          <div class="row g-3 mb-2">
            <div class="col-sm">
              <label class="copy-h6-format text-white">E-mail atual</label>
              <p class="form-control text-black"><%= USUARIO.email %> </p>
            </div>
          </div>
  
          <form action="/usuarios/alterar-email?_method=PUT" method="post" data-toggle="validator">
            <div class="row g-3 mb-2">
              <div class="col-sm">
                <label for="email" class="copy-h6-format text-white">Novo E-mail</label>
                <input type="email" name="email" class="form-control" id="email" placeholder="Digite o novo e-mail" required data-remote="http://localhost:3000/validar-email" />
              </div>
              <div class="col-sm">
                <label class="copy-h6-format text-white">Confirmar</label>
                <input
                  type="email"
                  name="confirmEmail"
                  class="form-control col-auto"
                  placeholder="Confirme o e-mail"
                  required
                  data-match="#email"
                  data-match-error="Atenção! Os e-mails não estão iguais."
                />
                <div class="help-block with-errors mt-3"></div>
              </div>
              <%- include('../components/error-alert', {error}) %>
            </div>
            <div class="my-3"><button type="submit" class="btn btn-primary">Alterar</button></div>
          </form>
        </div>

      </div>
    </div>
  </div>
</section>



<%- include('../components/foot') %>
